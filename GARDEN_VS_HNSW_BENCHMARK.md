# GARDEN vs HNSW 过滤搜索性能对比报告

测试日期：2026-08-06
测试程序：`test/index/garden_vs_hnsw_bench_test.cpp`
环境：单机，N=100000 条 128 维向量，k=10，每个选择性 200 次查询取平均

## 测试设计

对比两条过滤搜索路径：
- **HNSW**：全量图 + bitmap 后过滤（`RoaringBitmapIDFilter` 白名单，遍历时只保留 bitmap 里的 id）
- **GARDEN**：子图路由（按选择性分流：暴力/子图搜索/全量图）

brand 字段 K 个值均匀分布，查询 `brand=brand_5`，选择性 = 1/K。

## 结果

| 选择性 | candidate | HNSW QPS | GARDEN QPS | HNSW p50 | GARDEN p50 | 召回(两者) | GARDEN/HNSW |
|--------|-----------|----------|------------|----------|------------|-----------|-------------|
| 50%    | 50000     | 6191     | 6048       | 0.16ms   | 0.16ms     | 1.000     | 1.0x        |
| 10%    | 10000     | 2662     | 6446       | 0.37ms   | 0.15ms     | 1.000     | **2.4x**    |
| 2%     | 2000      | 671      | 293        | 1.49ms   | 3.41ms     | 1.000     | 0.4x        |

## 分析：GARDEN 的精确优势区间

**50% 高选择性（K=2）**：两者相当（1.0x）。候选集大，HNSW 全量图遍历也有效，GARDEN 子图无额外优势。

**10% 中选择性（K=10）：GARDEN 快 2.4 倍**。candidate=10000 恰好跨过 kBruteBound(10000) 且选择性 < kFullSearchRate(80%)，GARDEN 走**子图搜索**（brand_5 子图内全是满足条件的向量，图遍历高效），HNSW post-filter 要在全量图里过滤掉 90% 节点，慢。这是 GARDEN 的核心收益区间。

**2% 极低选择性（K=50）：GARDEN 慢 0.4 倍**。candidate=2000 < kBruteBound，GARDEN 走**暴力检索**——遍历 candidate_bitmap 的 2000 个候选，对每个做 `id_to_vector_.find`（std::map，O(log N)）+ 128 维距离计算，共 2000 次距离。HNSW post-filter 只对图遍历到的几百个节点算距离（近似搜索），反而快。

## 关键结论

GARDEN 的优势**不是所有过滤都更快**，而是精确的**中等选择性区间**（candidate ≥ kBruteBound 且选择性 < kFullSearchRate，即 1%-80% 之间）走子图时更快：

- 中选择性（子图路径）：GARDEN 赢 2.4x ← 核心卖点
- 高选择性（全量图）：两者相当
- 极低选择性（暴力路径）：GARDEN 反而慢（暴力对所有候选算距离，HNSW 图遍历只算部分）

## 可优化点（如需进一步提升）

K=50 暴力路径慢的根因：暴力对所有候选（2000）算距离 + std::map find 开销。若要优化，可在极低选择性也尝试走子图（子图内候选少，HNSW 遍历子图比暴力算全部候选快），或把 `id_to_vector_` 从 std::map 换成连续存储/预取友好的结构。但这改变 GARDEN 的 kBruteBound 设计假设，需权衡。

## 面试价值

这组数据量化了 GARDEN 的**精确生效区间**，比"GARDEN 比 HNSW 好"有说服力得多：

> 压测发现 HNSW 在带标量过滤时，中选择性（10% 左右）会断崖（2662 QPS vs 无过滤的 6000+）。我实现的 GARDEN 用子图路由在这个区间恢复性能到 6446 QPS（2.4x），召回保持 1.0。但实测也发现它的边界：高选择性（50%）时全量图本来就有效所以两者相当，极低选择性（2%）时走暴力路径反而比 HNSW 慢（暴力对所有候选算距离）。所以 GARDEN 的价值在精确的中等选择性区间。

这是"发现问题 → 实现方案 → 实测验证收益区间和边界"的完整闭环。

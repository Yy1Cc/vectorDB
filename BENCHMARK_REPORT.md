# vectorDB 基准测试报告

**测试日期**: 2026-07-13 17:04:52

**服务地址**: http://127.0.0.1:7781

**测试配置**: 每组 200 条查询，k=100，128 维向量，10000 条数据

**测试索引类型**: FLAT、HNSW、SQ8

**测试维度**: 128、256、768

---

## 一、搜索性能测试

测量各索引类型的写入吞吐（batch_upsert 走 Raft 复制链路）、串行/并发搜索 QPS、延迟分位数（p50/p99）以及召回率（recall@100，与 numpy 暴力 L2 搜索的 ground truth 对比）。

| 索引类型 | 维度 | 数据量 | 写入耗时 (s) | 写入 QPS | 串行 QPS | 串行 p50 (ms) | 串行 p99 (ms) | 召回率@100 | 并发 QPS | 并发 p50 (ms) | 并发 p99 (ms) |
|---------|------|--------|-------------|---------|---------|--------------|--------------|-----------|---------|--------------|--------------|
| FLAT | 128 | 10000 | 6.08 | 1644 | 408.7 | 2.36 | 3.24 | 1.0000 | 521.9 | 7.48 | 12.02 |
| HNSW | 128 | 10000 | 25.25 | 396 | 334.1 | 2.95 | 3.54 | 0.7790 | 488.2 | 7.92 | 12.96 |
| SQ8 | 128 | 10000 | 6.17 | 1621 | 230.2 | 4.29 | 5.05 | 0.9269 | 463.8 | 7.88 | 14.98 |

### 分析

FLAT 作为精确搜索索引，召回率 1.0 完全符合预期，是精度基准。写入 QPS 达 1644（与 SQ8 接近），说明 Faiss IndexFlat 的批量插入路径高效。串行搜索 408.7 QPS、p50 仅 2.36ms，在 1 万条 128 维数据量下性能优秀。

HNSW 写入 QPS 仅 396，远低于 FLAT 和 SQ8，因为 HNSW 构图需要维护多层图结构，每条插入都要做邻边计算和连接更新。召回率 0.779 偏低，主要原因可能是 `ef_search` 参数设置偏小，导致搜索时图遍历不充分。并发搜索 488.2 QPS 相比串行 334.1 提升约 46%，并发效率尚可。

SQ8（8bit 标量量化）写入 QPS 1621 与 FLAT 接近，说明量化索引的 lazy train 机制在首次插入时自动训练开销可控。召回率 0.9269 是量化损失所致（128 维 float32 → int8 压缩 4 倍），在可接受范围内。串行搜索 230.2 QPS 低于 FLAT 的 408.7，因为量化索引搜索时需要反量化计算距离，额外解码开销导致。

---

## 二、整数范围过滤搜索测试

测试 `int_field >= N` 条件下的过滤搜索，分别在 1%（低选择性）、50%（中等选择性）、99%（高选择性）三个过滤率下测量。过滤准确率 = 返回结果中满足过滤条件的比例。

### FLAT 索引

| 过滤率 | 阈值 | QPS | p50 (ms) | p99 (ms) | 过滤准确率 |
|-------|------|-----|----------|----------|-----------|
| 1% | 100 | 121.1 | 8.15 | 10.48 | 1.0000 |
| 50% | 5000 | 130.0 | 7.59 | 8.55 | 1.0000 |
| 99% | 9900 | 431.5 | 2.30 | 2.60 | 1.0000 |

### HNSW 索引

| 过滤率 | 阈值 | QPS | p50 (ms) | p99 (ms) | 过滤准确率 |
|-------|------|-----|----------|----------|-----------|
| 1% | 100 | 110.8 | 9.07 | 10.74 | 1.0000 |
| 50% | 5000 | 105.1 | 9.54 | 10.59 | 1.0000 |
| 99% | 9900 | 64.5 | 15.12 | 21.31 | 1.0000 |

### SQ8 索引

| 过滤率 | 阈值 | QPS | p50 (ms) | p99 (ms) | 过滤准确率 |
|-------|------|-----|----------|----------|-----------|
| 1% | 100 | 99.1 | 10.03 | 13.53 | 1.0000 |
| 50% | 5000 | 110.6 | 8.99 | 11.47 | 1.0000 |
| 99% | 9900 | 368.2 | 2.64 | 3.62 | 1.0000 |

### 分析

所有索引类型在所有过滤率下过滤准确率均为 1.0，说明 `FilterIndex`（基于 RoaringBitmap）的标量过滤逻辑完全正确，不存在漏过滤或误过滤的问题。

FLAT 和 SQ8 的表现模式一致：99% 高选择性（过滤后只剩 1% 数据）时 QPS 反而最高（431.5 和 368.2），因为候选集小、距离计算少；1% 低选择性（过滤后剩 99% 数据）时 QPS 较低（121.1 和 99.1），因为需要计算大量候选向量的距离。这是"先过滤后搜索"策略的典型表现。

HNSW 表现相反：99% 高选择性时 QPS 最低（64.5），p99 达 21.31ms。这是因为 HNSW 图搜索在低选择性过滤时需要遍历大量被过滤节点才能找到满足条件的近邻，图遍历效率急剧下降。这正是 SimOL GARDEN 论文提到的"HNSW 在低选择性过滤下性能断崖"问题。当前 vectorDB 的 HNSW 过滤采用 bitmap 后过滤策略，尚未实现 ACORN 或多子图优化。

---

## 三、标签等值过滤搜索测试

测试 `labels == 'label_X'` 字符串等值过滤，使用带标签的数据（5% 和 20% 选择性），在 FLAT 索引上测试。

| 标签选择性 | 标签值 | QPS | p50 (ms) | p99 (ms) |
|-----------|-------|-----|----------|----------|
| 5% | label_5p | 439.0 | 2.25 | 2.87 |
| 20% | label_20p | 410.2 | 2.38 | 3.26 |

### 分析

标签等值过滤性能优秀，5% 选择性时 439 QPS、20% 选择性时 410 QPS，延迟 p50 均在 2.5ms 以内。相比无过滤的 FLAT 串行搜索（408.7 QPS），带标签过滤的搜索甚至更快，因为 RoaringBitmap 预过滤缩小了候选集，减少了距离计算量。标签过滤和整数过滤走的是同一套 FilterIndex + RoaringBitmap 机制，但等值过滤（`op="="`）比范围过滤（`op=">="`）更高效，因为 bitmap 的 AND 操作可以直接精确匹配。

---

## 四、多维度 Collection 共存测试

验证不同维度的 Collection（128、256、768）可以同时存在且搜索结果相互隔离。

| 维度 | 数据量 | 搜索返回结果数 | 状态 |
|------|-------|--------------|------|
| 128 | 2000 | 10 | 通过 |
| 256 | 2000 | 10 | 通过 |
| 768 | 2000 | 10 | 通过 |

### 分析

三个不同维度的 Collection 同时存在于同一 vectorDB 实例中，各自独立插入和搜索，结果互不干扰。这验证了多 Collection 架构的核心设计：`CollectionManager` 为每个 Collection 维护独立的 `IndexFactory`（含独立维度、度量、索引实例），写入和搜索请求通过 `collectionName` 路由到正确的 Collection。此前 vectorDB 只支持全局单一维度（配置文件中的 `DIM` 字段），现在每个 Collection 可自定义维度，大幅提升了灵活性。

---

## 五、总结

### 测试覆盖

本次测试覆盖了 vectorDB 的四项核心能力：搜索性能（3 种索引）、整数范围过滤（3 种索引 × 3 种选择性）、标签等值过滤（2 种选择性）、多维度 Collection 共存（3 种维度）。

### 关键发现

搜索性能方面，FLAT 精确搜索召回率 1.0、性能最优（408.7 QPS），适合对精度要求高的场景；SQ8 量化索引在 4 倍压缩比下召回率 0.927，性能损失约 44%（230.2 QPS），适合内存受限场景；HNSW 召回率 0.779 偏低，建议调大 `ef_search` 参数。

标量过滤方面，FilterIndex + RoaringBitmap 机制完全正确（过滤准确率 100%），整数范围过滤和标签等值过滤均工作正常。HNSW 在高选择性过滤下性能下降明显（99% 选择性时仅 64.5 QPS），这是 HNSW 图过滤的固有瓶颈，可考虑参考 SimOL GARDEN 的多子图策略优化。

多维度架构方面，多 Collection 架构工作正常，不同维度 Collection 隔离正确，解决了此前全局单维度的限制。

### 测试工具

VectorDBBench 适配器（`VectorDBBench/vectordb_bench/backend/clients/vectordb/`），基准测试脚本（`vectorDB/bench/run_benchmark.py`），通过 HTTP API 调用 vectorDB，numpy 暴力 L2 搜索计算 ground truth。
